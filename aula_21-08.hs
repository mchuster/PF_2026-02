double x = x + x
quadruple x = double (double x)

factorial n = product [1..n]

average ns = sum ns `div` length ns
teste = div 10 2
--div = 

--faca uma funcao que recebe uma tupla de inteiros e retorna um inteiro como
--add :: (Int, Int) -> Int
add :: (Int, Int) -> Int
add (x, y) = x + y